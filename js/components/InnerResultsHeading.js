const innerResultsHeading = (results, totalResults) => `
    <h1 style="padding-bottom: 20px; text-align: center;">
        ${results.length} of ${totalResults} Results
    </h1>
`

export default innerResultsHeading;